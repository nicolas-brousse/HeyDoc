<!doctype html>
<html lang="en">
<head>
    <meta charset="utf-8">

    <title>{{ page.title }}</title>
    {% if config.has('description') %}<meta name="description" content="{{ config.description }}">{% endif %}
    {% if config.has('author') %}<meta name="author" content="{{ config.author }}">{% endif %}

    <meta name="generator" content="HeyDoc">

    <style>
    {% include 'assets/css/app.css' %}
    </style>
</head>

<body>
    <h1>{{ page.title }}</h1>

    <div class="content">
        {{ content|raw }}
    </div>
</body>
</html>
